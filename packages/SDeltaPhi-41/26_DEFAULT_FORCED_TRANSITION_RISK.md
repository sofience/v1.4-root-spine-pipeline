# Default Forced Transition Risk

Default forced transition occurs when a default setting silently transforms another system's path.

## Example

```text
The default path was used -> the user chose it.
```

## Correction

Use Default Power audit and TCC for opt-out cost.
