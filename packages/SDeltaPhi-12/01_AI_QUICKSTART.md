# AI Quickstart

Use SΔϕ-12 when the task requires analysis of memory, forgetting, persistence, repeat failure, fixation, paralysis, or adaptive continuity under irreversibility.

## Primary route

```text
SΔϕ-01 → SΔϕ-03 → SΔϕ-09 → SΔϕ-10 → SΔϕ-11 → SΔϕ-12
```

## Root-spine compatible route

```text
SΔϕ-01 → SΔϕ-03 → SΔϕ-47 → SΔϕ-62 → SΔϕ-11 → SΔϕ-12
```

## Minimal decision rule

If an irreversible trace does not re-enter next selection, repeat risk increases.

If all traces re-enter, feasible path space collapses.

If selective trace re-entry and compression remain balanced, classify as adaptive persistence.
