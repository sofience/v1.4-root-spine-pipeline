# Routing Notes

SΔϕ-12 should be called after SΔϕ-11 when boundary re-entry requires persistence regulation.

## Primary route

```text
SΔϕ-01 → SΔϕ-03 → SΔϕ-09 → SΔϕ-10 → SΔϕ-11 → SΔϕ-12
```

## Root-spine compatible route

```text
SΔϕ-01 → SΔϕ-03 → SΔϕ-47 → SΔϕ-62 → SΔϕ-11 → SΔϕ-12
```

Do not call SΔϕ-12 merely because the word memory appears. If the task is about database memory, vector storage, retrieval infrastructure, or product memory features, use SΔϕ-12 only if irreversible trace regulation is structurally relevant.
