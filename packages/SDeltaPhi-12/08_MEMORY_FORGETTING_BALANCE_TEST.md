# Memory–Forgetting Balance Test

1. Check irreversible trace: `RollbackCost(Δϕ_t) >> UpdateBudget`.
2. Check memory strength: does the trace re-enter next transition selection?
3. Check forgetting strength: are constraints compressed or pruned enough to keep feasible path space open?
4. Check balance: repeat-risk below threshold and feasible path set non-collapsed.

```text
RepeatRisk < threshold
|Ω| > minimum
```
