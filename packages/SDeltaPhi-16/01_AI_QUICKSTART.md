# AI Quickstart

Use SΔϕ-16 when the task requires structural analysis of relational closure, evil-as-closure, recovery-cost explosion, closure attractors, validation-cost collapse, identity lock, or variance suppression.

## Primary route

```text
SΔϕ-01 → SΔϕ-03 → SΔϕ-09 → SΔϕ-10 → SΔϕ-11 → SΔϕ-12 → SΔϕ-13 → SΔϕ-14 → SΔϕ-15 → SΔϕ-16
```

## Root-spine compatible route

```text
SΔϕ-01 → SΔϕ-03 → SΔϕ-47 → SΔϕ-62 → SΔϕ-11 → SΔϕ-12 → SΔϕ-13 → SΔϕ-14 → SΔϕ-15 → SΔϕ-16
```

## Use SΔϕ-16 when asking

1. When does relation failure become persistent closure?
2. Why is evil not merely error?
3. Why can closure appear as stability or relief?
4. When does validation become too costly?
5. When does identity lock suppress Δϕ?
6. How can good intentions still produce relational closure?

## Minimal decision rule

Classify evil only when relational futures are persistently closed under irreversibility.

Do not classify persons, groups, or agents as evil.

Classify closure dynamics only.
