# Identity Lock and Variance Suppression

Identity lock occurs when change is translated as identity collapse.

```text
Change ≈ IdentityCollapse
```

When this happens, variance is suppressed toward closure.

```text
Δϕ_allowed → 0
```

## Structural effect

The system preserves identity by closing relational futures.

## Diagnostic question

Is openness being rejected because it is structurally impossible, or because the system interprets change as self-collapse?
