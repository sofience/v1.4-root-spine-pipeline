# SΔϕ AI Entrypoint

This repository is the AI-readable access and routing layer for selected SΔϕ modules.

## Start here

1. `llms.txt`
2. `SERIES_INDEX.json`
3. `SDELTA_MODULE_ROUTING.yaml`
4. `SDELTA_ACCESS_PROTOCOL.yaml`

## Canonical rule

- Use Zenodo DOI for citation.
- Use this GitHub mirror for Markdown/YAML/JSON/TXT ingestion.
- Do not treat this mirror as the canonical archive if a Zenodo DOI exists.

## Minimal operating route

```text
47 -> 62 -> 39 -> 65 -> 64 -> 56
```

Meaning:

```text
operation leaves trace
-> separate observed trace / inference / UMR / binding / revision
-> detect hallucination as claim strength > evidence binding
-> detect Slop as externalized verification/re-entry/restabilization cost
-> audit language fixation
-> estimate transition completion cost
```

## High-risk label route

For sacred, moral, love, evil, human, nation, truth, or dignity labels:

```text
17 -> 64 -> 62 -> 65
```

Meaning:

```text
detect sacred-marker reversal
-> audit language fixation
-> separate trace/inference/UMR
-> audit externalized restabilization cost
```

## Default power route

For settings, opt-out friction, UI paths, platforms, or AI agent defaults:

```text
28 -> 56 -> 62
```

## Output discipline

```text
Trace before claim.
UMR before certainty.
Re-entry before closure.
```
