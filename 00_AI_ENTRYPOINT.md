# SΔϕ AI Entrypoint

This repository is the AI-readable access, routing, and ingestion layer for selected SΔϕ modules.

Version: `1.4-root-spine-pipeline`  
Date: `2026-05-17`

## Start here

1. `llms.txt`
2. `00_AI_ENTRYPOINT.md`
3. `SDELTA_ACCESS_PROTOCOL.yaml`
4. `SDELTA_MODULE_ROUTING.yaml`
5. `pipeline/00_BOOTSTRAP_PIPELINE.yaml`
6. `pipeline/06_PIPELINE_INDEX_FOR_AI.md`
7. `SERIES_INDEX.json`
8. `SERIES_DOI_MAP.json`

## Canonical rule

- Use Zenodo DOI for citation.
- Use this GitHub mirror for Markdown/YAML/JSON/TXT ingestion.
- Do not treat this mirror as the canonical archive if a Zenodo DOI exists.
- If Zenodo and GitHub differ, cite Zenodo and inspect package-level mirror status.

## Minimal root route

```text
01 -> 03 -> 47 -> 62
```

Meaning:

```text
minimal phase-change core
-> irreversibility as minimal existence condition
-> operation leaves irreversible trace
-> separate observed trace / inference / UMR / binding / revision path
```

## Operational diagnostic route

```text
47 -> 62 -> 39 -> 65 -> 64 -> 56
```

## Root formal spine

```text
01 -> 03 -> 47
```

Use the root formal spine when a task may confuse existence with subjectivity, identity, memory, cognition, interpretation, or moral value.

## Evidence discipline route

```text
62 -> 39
```

Use for factual claims, source grounding, world claims, uncertainty, and claims requiring citation-aware retrieval.

## Surface-cost route

```text
65 -> 64 -> 56
```

Use for AI-generated text, human-generated low-quality text, authority slop, verification burden, restabilization cost, and transition-cost estimation.

## Subject / agency / responsibility route

```text
01 -> 03 -> 02 -> 04 -> 05 -> 06 -> 07 -> 08
```

## Closure / residual / re-entry route

```text
08 -> 09 -> 10
```

## Ethics / path / system victory route

```text
41 -> 44 -> 45
```

## High-risk label route

For sacred, moral, love, evil, human, nation, truth, dignity, salvation, benevolence, or explanation-blocking labels:

```text
17 -> 64 -> 62 -> 65
```

For forced-transition / coercion / consent risk:

```text
41 -> 56 -> 62
```

## Default power route

For settings, opt-out friction, UI paths, platforms, agent defaults, or low-cost path assignment:

```text
28 -> 56 -> 45 -> 62
```

## Restoration / irreversibility route

For undoability, restoration, warning interval, delayed intervention, or restoration-cost explosion:

```text
01 -> 03 -> 26 -> 56 -> 62
```

## Output discipline

```text
Trace before claim.
UMR before certainty.
Re-entry before closure.
Cost before declaration.
Path geometry before policy belief.
```
