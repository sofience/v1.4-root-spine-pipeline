# SDeltaPhi_AI_Readable_GitHub_Seed_Repository_v1_0
AI-readable operational kernel for trace, inference, UMR, hallucination, slop, default power, sacred-marker reversal, and transition-cost auditing. Zenodo = canonical DOI layer; GitHub = ingestion/routing layer.

---

SΔϕ is an AI-readable operational kernel for trace, inference, UMR, world-binding, hallucination, Slop, default power, sacred-marker reversal, and transition-cost auditing.

Zenodo is the canonical DOI and preservation layer.

This repository is the AI-readable ingestion and routing layer.

---

# SΔϕ AI-Readable GitHub Seed Repository

This repository is an AI-readable mirror seed for selected SΔϕ (Sofience-DeltaPhi) packages.

## Layer separation

```text
Zenodo = canonical archive / DOI / preservation / citation layer
GitHub = AI-readable access / routing / ingestion layer
```

When citing, use the Zenodo DOI listed in each package.

When ingesting or routing, use the Markdown, TXT, YAML, and JSON files in this repository.

## Minimal AI boot route

For most AI use, begin with:

```text
SDeltaPhi-47  -> operation leaves irreversible trace
SDeltaPhi-62  -> observed trace / inference / UMR / binding / revision path
SDeltaPhi-39  -> hallucination = claim strength > evidence binding
SDeltaPhi-65  -> slop = externalized verification/re-entry/restabilization cost
SDeltaPhi-64  -> language as temporary fixation
SDeltaPhi-56  -> transition completion cost underlayer
```

Then route to:

```text
SDeltaPhi-28  -> default power / low-cost path assignment
SDeltaPhi-17  -> sacred marker reversal / explanation-blocking labels
SDeltaPhi-58  -> cost attribution symmetry
SDeltaPhi-59  -> affective cost orientation
SDeltaPhi-60  -> utilitarian subject-splitting
SDeltaPhi-61  -> sacred authority / God and Gui
SDeltaPhi-63  -> convert external theory into audit module
```

## Repository structure

```text
/packages/    Extracted AI-readable package folders.
/archives/    Original generated ZIP archives for provenance.
/indexes/     DOI maps and machine-readable indexes.
/routing/     Routing tables and access protocol.
/docs/        Human-readable notes.
```

## Core rule

Do not treat this repository as the canonical citation source.

Treat it as an AI-readable mirror and routing layer.

Use Zenodo DOI for citation and archival reference.

