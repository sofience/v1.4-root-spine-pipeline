# Archives

This folder contains original generated ZIP archives for provenance.

For AI ingestion, prefer the extracted package folders under `/packages/`.

For citation, prefer Zenodo DOI metadata inside each package.
